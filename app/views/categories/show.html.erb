<div class="container" style="margin:0 auto;">
	<p id="notice"><%= notice %></p>

  
	<h1 style="color:<%= @category.color %>;"><%= @category.name %></h1>
	<h2>Artículos</h2>

	<div class="row">
		<%= render @category.articles %>
	</div>

	<%= link_to 'Edit', edit_category_path(@category), style:"color:#004d40; font-size:20px" %> |
	<%= link_to 'Back', categories_path, style:"color:#004d40; font-size:20px" %>

</div>
